export const CONFIG_FILENAMES = [
  "geo.json",
  ".geo.json",
  "geo.config.json",
] as const;
export const DEFAULT_TIMEOUT = 45000;
export const DEFAULT_USER_AGENT = "GEOAudit/0.1.0";
export const DEFAULT_FORMAT = "pretty" as const;
